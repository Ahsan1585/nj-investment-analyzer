NJ Investment Property AnalyzerThis is a single-page, responsive web application designed to help real estate investors analyze New Jersey commercial and multi-family property listings under a $2M price cap.The application includes:Live Property Listings: A table of sample listings (data stored in Firebase Firestore) filtered by Cap Rate, Occupancy, and Property Type.Commercial Mortgage Calculator: Automatically populates with data when a listing is clicked, allowing for instant P&I calculation based on current commercial rates.Firebase Integration: Uses Google's Firebase Firestore for public data storage, ensuring real-time data updates and persistence.🚀 How to Publish to GitHub PagesSince this project is a single-page application (index.html), it's incredibly easy to deploy using GitHub Pages:Create a Repository: Log in to GitHub and create a new public repository (e.g., nj-investment-analyzer).Upload Files: Upload the two files generated here (index.html and .gitignore), along with this README.md, to the root of your new repository.Enable GitHub Pages:Go to your repository's Settings tab.In the sidebar, click on Pages.Under Source, select the main branch (usually main or master) and the root directory (/).Click Save.Access App: GitHub will build the site and provide you with a live URL, typically at https://yourusername.github.io/nj-investment-analyzer/. This usually takes less than one minute.⚙️ Project StructureThe entire application logic (HTML, Tailwind CSS, JavaScript, and Firebase integration) is contained within the single file:index.html: The complete, standalone application..gitignore: Standard file to exclude temporary or environment-specific files.🔒 Firebase ConfigurationThis application uses the shared Canvas environment configuration for Firebase. If you move this to a private, production environment, you will need to:Set up your own Firebase project.Replace the placeholder values for __app_id, __firebase_config, and __initial_auth_token in index.html with your actual public Firebase credentials.